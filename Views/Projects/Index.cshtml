@model IEnumerable<ProjectDipMVC.Models.Project>

@{
    ViewData["Title"] = "Проекты";
}

<h1>Проекты</h1>

<p>
    <a asp-action="Create">Создать новый</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateCreate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TitulName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.ActionLink(item.Name, "Index", "ProjectDescripts", new {ProjectId = item.ProjectId})
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateCreate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TitulName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProjectId">Редактировать</a> |
                <a asp-action="Details" asp-route-id="@item.ProjectId">Подробности</a> |
                <a asp-action="Delete" asp-route-id="@item.ProjectId">Удалить</a> |
                <a asp-action="Build" asp-route-id="@item.ProjectId">Собрать</a>
            </td>
        </tr>
}
    </tbody>
</table>
